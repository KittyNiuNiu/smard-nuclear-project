version: 2

models:
  - name: stg_smard_timeseries
    description: "Cleaned, deduplicated SMARD timeseries with proper timestamps and Berlin local date."
    columns:
      - name: filter_id
        description: "SMARD filter code"
        data_tests:
          - not_null
          - accepted_values:
              values: [1223, 1224, 1225, 1226, 1227, 1228, 4066, 4067, 4068, 4069, 4070, 4071, 410, 4169, 254]
              quote: false

      - name: region
        description: "Region code"
        data_tests:
          - not_null
          - accepted_values:
              values: ['DE']

      - name: date
        data_tests:
          - not_null

      - name: mwh
        description: "Generation/consumption in MWh per day, or price in EUR/MWh. Never NULL in this model."
        data_tests:
          - not_null